#include<stdio.h>
void main()
{
	int side,area,perimeter;
	
	side = 2;
	area = side*side;
	perimeter = 4*side;
	
	printf("area of the square with side 2 is %d\n", area);
	printf("perimeter of the square with side 2 is %d", perimeter);
}
